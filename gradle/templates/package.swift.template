// swift-tools-version:5.3
import PackageDescription

let package = Package(
   name: "$MODULE",
   platforms: [
     .iOS(.v14),
   ],
   products: [
      .library(name: "$MODULE", targets: ["$MODULE"])
   ],
   targets: [
      .binaryTarget(
         name: "$MODULE",
         url: "$XCFRAMEWORK_URL",
         checksum:"$CHECKSUM")
   ]
)
